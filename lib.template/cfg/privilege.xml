<?xml version="1.0" encoding="koi8-r"?>
<!-- issue.privilege-rework: membership-driven privileges (cfg/privilege.xml).
     SERVER-EDITED ONLY - intentionally NOT exposed to the Vedun in-game editor (security).
     <god_list> sections are listed highest-seniority first (readability only).
     This repo file is a minimal stub; the production list lives outside the repo and is generated
     from misc/privilege.lst by tools/privilege_lst_to_xml.py. See OUTPUT/issue.privilege-rework-plan.md.

     Entry shape:
       <entry name="п≤п╪я▐" uid="12345">
         <flags    list="skills|boards|FullAccess"/>   capability tokens; FullAccess = omnipotent
         <groups   list="Goto|Olc"/>                    references to <command_groups> ids
         <commands list="invis|set (email frozen)|show (zones errors)"/>
       </entry>
-->
<privileges>
    <command_groups>
        <!-- reusable bundles, e.g.:  <group id="Mute" commands="mute|set (mute)"/> -->
    </command_groups>
    <god_list>
        <owner>
            <!-- exactly one owner honoured; name+uid ignored (owner hardcoded in the module) -->
        </owner>
        <implementators/>
        <great_gods/>
        <gods/>
        <demigods/>
    </god_list>
</privileges>
